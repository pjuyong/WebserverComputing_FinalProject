CREATE TABLE club_info (     id INT AUTO_INCREMENT PRIMARY KEY,     category VARCHAR(50) NOT NULL,     content TEXT NOT NULL );
 
 
INSERT INTO club_info (category, content) VALUES ('동아리규칙', '연애금지');
